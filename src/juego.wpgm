import wollok.game.*
import menu.*
import nivel.*

program juego {
	game.title("Juego Base")
	game.cellSize(100)
	game.addVisual(fondo)
	game.boardGround("fondoCiudad.jpeg")
	game.height(5)
	game.width(13)
	menu.iniciarMenu()
    game.start()
}
